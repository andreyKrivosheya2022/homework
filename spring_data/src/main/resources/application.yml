server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/data_access
    username: root
    password: hayday0992717546
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  logging:
    level:
      org.springframework.web: INFO
      com.example.spring_data: INFO
    file:
      name: myapp.log
      path: /var/log/myapp/
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{15} - %msg%n"
      file: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{15} - %msg%n"
